import { css } from '@emotion/react';

export const textLabel = css`
  overflow: hidden;

  display: inline-block;
  flex-direction: row;
  align-items: center;
  min-width: 100px;

  font-family: inherit;
  font-size: 12px;

  padding-left: 8px;
`;
